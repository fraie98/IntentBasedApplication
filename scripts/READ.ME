Those are python2 and bash scripts.

Python2 scripts need requests, matplotlib, python-tk, scipy which can be installed with the following commands:
sudo apt install python-pip
pip install requests
pip install matplotlib
pip install scipy
sudo apt-get install python-tk
